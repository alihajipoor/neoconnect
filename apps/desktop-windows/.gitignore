# Cargo build output. Lives here rather than in src-tauri/ because the
# three Rust crates (src-tauri, service, ipc) share one workspace rooted
# at this directory, so they also share one target directory.
/target/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
